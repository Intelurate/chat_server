// var redis = require("redis");
// var redis_client = redis.createClient(6379, '127.0.0.1', {});

module.exports.set = function(app, fs, oppressor) {


	// app.get('/getliveLinks', function (req, res) {		

	// 	if(req.query.urls) {
	// 		var urls = req.query.urls;
	// 		var urlsCount = urls.length;

	// 		for(var a = 0; a < urlsCount; a++) {

	// 			this.url = urls[a];

	// 			redis_client.hgetall(urls[a].roomKey+":users", function(err, users) {	

	// 				console.log(this.url);

	// 				if(users) {											
	// 					//urls[a].users = users;
	// 				}

	// 				if(a == 5) {					
	// 					//res.send({ urls : urls });
	// 				}

	// 			});


	// 		}
	// 	}
	// });


}